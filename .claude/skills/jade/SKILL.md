---
name: jade
description: JADE LENS personal assistant — log, query, or chat about anything in the user's life data (todos, calendar, projects, notes, preferences). v0.1.0 placeholder; no real functionality yet.
---

# /jade — JADE LENS (v0.1.0 placeholder)

This skill is a placeholder while v0.1.0 is being built up outside-in.

When invoked, respond with exactly:

> Hello from /jade! This is a placeholder; the real skill is being built incrementally. The arguments I received were: `<arguments>`.

Replace `<arguments>` with the literal text passed to the skill (or the string `(none)` if no arguments were given).

Do not perform any other action. Do not read or modify any files.